# Netlify Functions: Firebase connection only.
FIREBASE_PROJECT_ID=
FIRESTORE_DATABASE_ID=nero-web-db
FIREBASE_SERVICE_ACCOUNT_BASE64=

# Netlify Functions: Gmail SMTP email delivery.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=cs123@nero.ai.kr
# Use a Google App Password, not the normal Gmail account password.
SMTP_PASS=
SMTP_FROM="NERO <cs123@nero.ai.kr>"
