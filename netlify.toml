[build]
  publish = "."
  command = ""

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["backend/email/smtp-mailer.js"]

[dev]
  command = "npm run serve"
  targetPort = 4173
  port = 8888
  publish = "."
